# Clean Core Graph Curation

The original `graph.json` remains the immutable Graphify extraction. `graph.curated.json` is a deterministic runtime/exploration refinement generated by `npm run clean-core:graph`.

| Metric | Source | Curated |
|---|---:|---:|
| Nodes | 251 | 246 |
| Links | 369 | 371 |
| Zero-degree nodes | 5 | 0 |
| Merged alias IDs | 0 | 5 |

## Curated bridges

- `sap_discovery_center_missions` -[supports]-> `side_by_side_extensibility`
- `abap_restful_application_programming_model` -[implements]-> `developer_extensibility`
- `sap_cloud_erp_private_advisory` -[supports]-> `clean_core_extensibility_governance`

These bridges connect previously zero-degree high-level concepts. They are marked `confidence=CURATED` and never authorize execution. Runtime decisions use `decision-rules.json` plus live evidence. Graphify's original report also labels 94 nodes as weakly connected; they remain an exploration-quality signal rather than an execution gap.
